# Vibecoding 全栈环境变量示例（复制为 .env 后按需修改）
# 用于 docker-compose.aws.yml 及本地运行

# ---------- 用户注册接口（User_Registeration）---------
SECRET_KEY=请改为随机长字符串且不要提交到仓库
DATABASE_URL=postgresql://postgres:postgres@db:5432/miniapi
REDIS_URL=redis://redis:6379/0

# 生产建议：限制 CORS 来源，逗号分隔，如 https://yourdomain.com,https://app.yourdomain.com
# CORS_ORIGINS=https://yourdomain.com
# 登录/注册限流：每分钟每 IP 最多请求数
# RATE_LIMIT_AUTH_PER_MIN=10

# ---------- AgentChat ----------
OPENAI_API_KEY=sk-你的密钥
# OPENAI_BASE_URL=http://localhost:11434/v1
# LLM_MODEL=gpt-4o-mini

# 后台看板、监控台与 /api/admin/status、/api/monitor/aggregate 鉴权（生产必设）
# ADMIN_API_KEY=随机长字符串

# 统一监控台：用户注册服务 base URL，配置后 /monitor 会聚合该服务状态（本地 http://localhost:8000，Docker 内 http://user-reg-app:8000）
# MONITOR_USER_REG_URL=http://user-reg-app:8000

# 聊天限流：每用户/IP 每分钟最多消息数
# RATE_LIMIT_CHAT_PER_MIN=20
# CORS_ORIGINS=https://yourdomain.com

# ---------- 可选 ----------
# TZ=UTC
# PIP_INDEX_URL=https://pypi.tuna.tsinghua.edu.cn/simple
