# AgentChat 环境变量示例（复制为 .env 后修改）

# LLM：OpenAI 或兼容接口
OPENAI_API_KEY=sk-xxx
# 使用 Ollama 时可不填 KEY，只填 BASE_URL
# OPENAI_BASE_URL=http://localhost:11434/v1
# LLM_MODEL=gpt-4o-mini

# Mem0 记忆（默认开启）
# MEM0_ENABLED=true

# RAG（默认关闭，可接入 UltraRAG 等）
# RAG_ENABLED=false
# RAG_TOP_K=3

# 服务
# HOST=0.0.0.0
# PORT=8765

# 生产：后台看板鉴权（设置后 /admin 拉取状态需输入 Admin API Key）
# ADMIN_API_KEY=随机长字符串
# 聊天限流：每 user_id/IP 每分钟最多消息数，0=不限制
# RATE_LIMIT_CHAT_PER_MIN=20
# CORS：逗号分隔域名，如 https://yourdomain.com
# CORS_ORIGINS=https://yourdomain.com
# CHAT_MESSAGE_MAX_LENGTH=8192
# CHAT_HISTORY_MAX_ITEMS=50
